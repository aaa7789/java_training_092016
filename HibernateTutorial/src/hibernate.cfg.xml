<?xml version='1.0' encoding='utf-8'?>
<!--
  ~ Hibernate, Relational Persistence for Idiomatic Java
  ~
  ~ License: GNU Lesser General Public License (LGPL), version 2.1 or later.
  ~ See the lgpl.txt file in the root directory or <http://www.gnu.org/licenses/lgpl-2.1.html>.
  -->
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/sample</property>
        <property name="connection.username">root</property>
        <property name="connection.password">bittu1</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">10</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup   create  will create tables , update will updated the schema-->
        <property name="hbm2ddl.auto">create</property>

       <!--  <mapping resource="com/mms/hibernate/basic/Event.hbm.xml"/>
         -->
           <!-- Names the annotated entity class -->
       <!--  <mapping class="com.mms.hibernate.annotations.Event"/>
        <mapping class="com.mms.hibernate.annotations.School"/>
        <mapping class="com.mms.hibernate.annotations.SchoolDetails"/> 
        <mapping class="com.mms.hibernate.associations.Person"/>
        <mapping class="com.mms.hibernate.associations.Phone"/>
        -->
        <mapping class="com.mms.hibernate.associations.Laptop"/>
        <mapping class="com.mms.hibernate.associations.LaptopDetails"/>

    </session-factory>

</hibernate-configuration>